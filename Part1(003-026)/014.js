const string = 'hello';
const string2 =
  'The method then selects elements form the start argument, and up to (but not including) the end argument.';

console.log(`string: ${string.length}`);
console.log(`string2: ${string2.length}`);
console.log(`string[0]: ${string[0]}`); //배열이 아니여도 불러올 수 있다
console.log(`string2[104]: ${string2[104]}`);
